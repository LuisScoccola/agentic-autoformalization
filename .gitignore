.env
# Ignore everything in workspace
workspace/**
# But don't ignore the directory itself (required so the next rule works)
!workspace/
# Don't ignore the specific subdirectory
!workspace/pythoformalize/
!workspace/pythoformalize/**

workspace/pythoformalize/__pycache__/**


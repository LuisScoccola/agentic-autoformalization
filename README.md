
# Installation

TODO: abstract /Users/luisscoccola
TODO: mention .env

You should only need to run the following once.

1. Run `./docker/compile.sh`.
2. Start the Docker server.
    - Run `./docker/serve.sh`.
    - From within that session, run `claude` and authenticate.
    - Close the session.
